rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // --- EXTREMELY PERMISSIVE - FOR DEBUGGING ONLY - DO NOT USE IN PRODUCTION ---
    match /{document=**} {
      allow read, write: if true; // Allow anyone to read and write anything
    }
  }
}